# macOS
.DS_Store

# Xcode per-user state
xcuserdata/
*.xcuserstate

# Build artifacts
build/
DerivedData/

# CocoaPods — vendored pods are not tracked; run `pod install` after cloning
Pods/

# Local-only working files (not part of the repo)
NEXT_STEPS.md
